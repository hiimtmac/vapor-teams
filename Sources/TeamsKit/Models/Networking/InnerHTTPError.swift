import Foundation

/// Object representing an inner HTTP error.
public struct InnerHttpError {
    /// HTTP status code from the failed request.
    public let statusCode: Int
    /// Body from the failed request.
//    let body: object
}
